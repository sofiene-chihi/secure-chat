import { ThemePalette } from "@angular/material/core";
import { ProgressSpinnerMode } from "@angular/material/progress-spinner";

export const spinnerMode : ProgressSpinnerMode= 'indeterminate'
export const spinnerValue=50
